# updating the mobileprovision file
-> open the .xcworkspace file from the platform/ios/<projectname>.xcworkspace in xcode project.
-> goto build setting tab then reach the provisioning profile if any existing then copy the code and seach the file on drive of users library (a hidden file) then mobile device then provisioning profile then search by file name and delete.
-> click on the provisioning profile and open it.
